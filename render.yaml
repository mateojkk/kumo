services:
  - type: web
    name: kumo-agent
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: PORT
        value: 3000
